import { Card } from "corestone";

export function Default() {
  return (
    <Card className="max-w-sm">
      <h3 className="text-h3 text-on-surface">Auditoría técnica</h3>
      <p className="mt-2 text-body text-on-surface-muted">
        Revisamos tu stack actual y entregamos un plan priorizado en dos semanas.
      </p>
    </Card>
  );
}
